## 1. 收敛共享契约与验证基线

- [x] 1.1 盘点并统一 `packages/shared`、前端类型、后端响应和现有测试中的用户类型、消息类型、SSE 事件与错误响应结构
- [x] 1.2 修正前后端测试夹具与 mock 数据，使其与统一后的共享契约一致
- [x] 1.3 建立并运行最小验证基线，确认前后端测试命令与校验脚本都能真实执行

## 2. 修复聊天执行主链路

- [x] 2.1 为聊天路由补充会话历史加载验证，先覆盖“已有历史消息继续聊天”的场景
- [x] 2.2 实现聊天路由向 runtime 传递“历史消息 + 当前消息”的完整上下文
- [x] 2.3 为 runtime 增加 checkpoint 恢复场景验证，覆盖“恢复旧状态但保留当前输入”
- [x] 2.4 修复 runtime 的 checkpoint 合并逻辑，避免旧状态覆盖本次消息
- [x] 2.5 为 provider 和 runtime 增加工具调用循环验证，覆盖工具请求、工具结果回注和上限保护
- [x] 2.6 补齐 OpenAI provider 的真实工具调用协议与无密钥 mock 行为
- [x] 2.7 为 SSE 失败路径补测试，覆盖“已开始流式输出后发生执行异常”
- [x] 2.8 修复聊天流错误处理，确保写出 SSE 后统一通过 `error` 事件结束

## 3. 修复认证、会话与聊天页面用户流

- [x] 3.1 为注册成功、未登录访问聊天页和登出行为补充前后端验证
- [x] 3.2 收敛注册即登录流程，修正注册成功后的前端跳转与页面文案
- [x] 3.3 补齐聊天页未登录处理与会话恢复逻辑，避免进入不可用页面
- [x] 3.4 将侧边栏登出入口接入真实登出行为并验证会话失效结果
- [x] 3.5 修复会话列表加载条件与数据流，确保登录后稳定展示历史会话
- [x] 3.6 实现可区分的会话标题生成或更新策略，并验证多个会话可被稳定区分
- [x] 3.7 统一历史消息与流式新消息的展示逻辑，验证消息顺序与展示一致性

## 4. 收尾工程门禁与文档

- [x] 4.1 修复 Prisma 查询日志配置，使其与当前日志监听逻辑一致
- [x] 4.2 将根级和子应用脚本收敛为真实可执行的测试与静态检查入口
- [x] 4.3 更新 README，使启动、测试、环境变量和脚本说明与仓库现状一致
- [x] 4.4 运行并记录本次变更所需的最终验证命令，确保实现进入可交付状态
