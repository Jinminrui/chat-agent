## 1. 渲染方案准备

- [x] 1.1 确认前端 Markdown 渲染依赖与最小配置，明确不启用原始 HTML 的实现方式
- [x] 1.2 梳理现有 assistant 历史消息与流式 `delta` 的展示入口，确定可复用的消息内容组件边界

## 2. 测试与验证基线

- [x] 2.1 为 assistant 历史消息补充渲染测试，覆盖标题、列表、链接、行内代码或代码块等 Markdown 场景
- [x] 2.2 为流式回复补充展示测试，验证生成中与完成后的 assistant 内容遵循一致的渲染语义
- [x] 2.3 为安全边界补充测试，验证 assistant 内容中的原始 HTML 不会被直接作为 HTML 渲染

## 3. 前端实现

- [x] 3.1 新增 assistant 消息 Markdown 展示组件，并封装基础样式约束
- [x] 3.2 更新消息列表展示逻辑，让 assistant 历史消息使用 Markdown 渲染、user 消息继续保持纯文本
- [x] 3.3 更新会话页流式展示逻辑，让 streaming `delta` 复用同一套 assistant Markdown 渲染能力

## 4. 回归验证

- [x] 4.1 运行前端相关测试并修复回归，确认历史消息、流式消息和响应式布局表现正常
- [x] 4.2 手动验证聊天页面，确认 Markdown 渲染效果、长内容换行和代码块滚动符合预期
