{
  "name": "web",
  "private": true,
  "scripts": {
    "dev": "echo \"web dev entry not configured yet\"",
    "lint": "echo \"lint not configured yet\"",
    "test": "echo \"test not configured yet\""
  }
}
