{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "echo \"backend dev entry not configured yet\"",
    "lint": "echo \"lint not configured yet\"",
    "test": "echo \"test not configured yet\""
  }
}
